//array methods of javascript
const arr=[1.2,2,5,"abcd",4]
arr.push(7)   //use to insert element at end  
arr.unshift(4); //use to insert element at the end 
arr.shift()
for(const item of arr)
{
    console.log(item)
}